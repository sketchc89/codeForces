#include <bits/stdc++.h>

using namespace std;

int main() {
    int64_t tests;
    cin >> tests;
    vector<int64_t> vi(tests);
    for (int i = 0; i < vi.size(); ++i) {
        int res = 0;
        vi[test] = res;
    }
    for (auto num : vi) {
        cout << num << '\n';
    }


    return 0;
}
