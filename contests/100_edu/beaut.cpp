/*
LANG: C++14
*/
#include <bits/stdc++.h>
using namespace std;


int main() {
    // i/o
    cin >> tests;
    vector<vector<int>> res(tests);
    for (int test = 0; test < tests; ++test) {
        int N;
        cin >> N;
        vector<int> a(N);
        for (int i = 0; i < a.size(); ++i) {
            cin >> a[i];
        }
        // res[test] =
    }
    return EXIT_SUCCESS;
}
